thread_pool_manager.gd is a autoload remember to set it as such

load_scene_with_interactive() is by desgin ment to run once per time don't use in in parallel if you do, expext unexpected behavor and crashes
